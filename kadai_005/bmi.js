// 変数の宣言・値の代入
// let bmi = 68 / (1.7 * 1.7);

// 変数-体重-の宣言
let weight = 68;

// 変数-身長-の宣言
let height = 1.7;

// 変数-BMI-の宣言
let bmi = weight / (height * height);

// コンソールへの出力
console.log(bmi);